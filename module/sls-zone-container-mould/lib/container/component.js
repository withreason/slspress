'use strict';


/**
 * Represents a components in the container.
 * Subclasses must have a one arg constructor that takes a container.
 * Components may have a start and or stop method to have their lifecycle managed.
 */
class Component {}

module.exports = Component;